# Variáveis de Ambiente para Docker

# URL base da API (usada no código)
VITE_BASE_URL_API=/api

# Target do proxy em desenvolvimento (opcional)
# Para desenvolvimento local: http://localhost:3000
# Para Docker: http://backend:3000
VITE_PROXY_TARGET=http://localhost:3000
