# Variáveis de Ambiente para Docker

# URL base da API (usada no código)
VITE_BASE_URL_API=/api

# Target do proxy em desenvolvimento
# Para Docker no Windows/Mac: http://host.docker.internal:3000
# Para Docker no Linux: http://localhost:3000
VITE_PROXY_TARGET=http://host.docker.internal:3000
